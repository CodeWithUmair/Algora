# Live session trade store (per-machine, not shared)
nasdaq_trades.sqlite
nasdaq_trades.sqlite-journal
nasdaq_trades_*.csv

# News calendar cache (runtime, refetched every 30 min - not shared)
.news_calendar_cache.json

# Streamlit process output when launched headless (runtime, not shared)
streamlit_output.log

# Python
__pycache__/
*.pyc
venv/
.venv/
.env

# Gold bots: per-timeframe trade DBs + runtime logs + pulled market data (per-machine)
gold_*_trades.sqlite
gold_*_trades.sqlite-journal
logs/
trading_bot/data_cache/
